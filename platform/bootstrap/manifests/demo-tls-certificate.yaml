apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-localhost
  namespace: default
spec:
  secretName: demo-localhost-tls
  issuerRef:
    name: local-ca
    kind: ClusterIssuer
  dnsNames:
    - demo.localhost
